# Changelog

- 压缩进行中提示行直接显示「正在压缩上下文（观察/反思）…」主文案（不再显示「上下文已压缩」标题）
- 压缩卡片统计行精简：去掉「压缩前/压缩后」前缀，数字按 k/w/M 单位化展示
- 观察压缩按 token 边界分块并行摘要（observeChunkTokens 默认 30000，完整消息不跨块），各块合并为一个 <history> 块提交；提示词按块位次分档（前块简单摘要、最后一块越往后越细）
- 新增配置 observeChunkTokens / observeChunkMaxTokens（观察分块单块摘要生成上限，默认 5000）
- summary 载荷 attemptCount 改为重试次数语义（观察分块为各块重试之和，反思为尝试次数 - 1），卡片直接展示该值；摘要粒度提示词补充「用户消息始终逐条保留原文」
