# Changelog

## [0.0.28] - 2026-09-06

## Unreleased

### Fixed

- om 私有会话事件（警告与观察压缩待定/失效标记）不再以自定义 `om/xx` 事件类型写入会话日志：宿主持久化读取路径拒绝目录外未知类型，导致含这些事件的会话无法加载；现借用宿主已知类型 `feedback/record` 以 `om:1:` 前缀 JSON 信封承载，历史会话加载恢复（旧 `om/xx` 事件不迁移，含旧事件的会话仍需处理）
