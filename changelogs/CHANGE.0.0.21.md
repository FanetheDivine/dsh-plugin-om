# Changelog

## [0.0.21] - 2026-09-02

- 新增配置 `observeChunkParallelism`（默认 2）：观察分块压缩的最大并行数，最多该数量的块同时摘要、其余块排队（`1` 即串行），结果仍按块顺序合并
- 新增配置 `rateLimitWaitMs`（默认 60000）：任一摘要请求遇 429 限流后，插件全局进入冷却期，后续所有摘要请求（含并行中的其他块）发出前至少等待该毫秒数（等待期间中止立即放弃，冷却期内新的 429 顺延冷却期；`0` 视为不限流）
